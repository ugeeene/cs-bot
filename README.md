The most striking differences are the smaller network size, the use of identity instead of ReLU, switching from SGD to Adam, and allowing the model to train much longer before stopping. The smaller architecture suggests that the dataset does not require a highly complex model. The use of identity activation implies that the problem may be close to linearly separable, meaning strong nonlinear transformations were not necessary. Removing regularization also makes sense given the reduced model complexity.
In terms of training behavior, my original model likely stopped very quickly due to the extremely low patience value, possibly before fully converging. Combined with SGD, this may have resulted in noisier updates and less stable convergence. The optimized model, using Adam, likely showed smoother and faster convergence. The higher maximum number of iterations and larger patience allowed the model to properly minimize the loss before stopping.
Regarding performance, the grid search model most likely achieved better validation and test performance, with improved generalization and more stable training dynamics. Overall, the optimized configuration demonstrates that a simpler architecture with adaptive optimization and more relaxed early stopping can outperform a larger, more aggressively regularized model.


The optimal hyperparameters found by scikit-learn were: hidden layer size 8 (i.e., [8,8]), identity activation, alpha = 0, Adam optimizer, learning rate 0.005, max_iter = 200, batch size = auto, early stopping enabled with n_iter_no_change = 37, and tol = 0.0001.
After plugging these parameters into the custom network, the following results were obtained: training accuracy 93.87%, validation accuracy 71.04%, training loss 0.24, validation loss 0.791, test loss 0.774, and test accuracy 70.50%.

Compared to the original model (test accuracy 78.03%, validation accuracy 76.23%, test loss 1.05), performance became worse after using the optimized hyperparameters. Although the training accuracy remained similar (around 93–94%), both validation and test accuracy decreased noticeably.

A likely explanation is that the optimized model is too simple for this specific implementation. The smaller hidden layer size ([8,8]) and identity activation reduce the model’s representational power, making it closer to a linear model. While this configuration may have performed well within scikit-learn’s framework (possibly due to differences in initialization, optimization details, or preprocessing), it appears to underfit slightly in the custom implementation. In contrast, the original model with 20 neurons and ReLU activation had higher capacity and achieved better generalization performance.

Thus, in this case, the scikit-learn optimal parameters did not improve performance in the custom network, likely due to differences in implementation details and reduced model complexity.



An example of a real-world harmful misclassification could be an automated hiring systems which makes use of an AI algorithm to screen through resumes of potential applicants.
The system might have been trained based on historical data that could potentially favor certain genders or ethnicities over the others. Thus, it might result in an incorrect classification of equally qualified applicants from underrepresented groups and mark them as less suitable.
As a consequence this can lead to an unfair rejection of talented individuals and further promote inequality in the workplace.
Misclassification here won’t just affect one person, but it might lead to a systematic disadvantage of entire groups in the long term. This can have an impact on more than just the economic aspects, also influencing social issues.

One way to solve the problem is to carefully audit and balance the training data to make sure that it includes the representation of all groups fairly. This will lead to a reduction of the bias that could have affected the classifier and improve the fairness of predictions. Another approach is implementing some human oversight in way that the decisions made by the algorithm are reviewed by humans, especially in when it comes to important decisions, e.g. hiring, lending or justice.
Even though these approaches may reduce the risk, they cannot completely solve the problem. This is because biases can still remain present or the data can change over time. Also, the inequalities and bias might exist outside the model itself. As a result, it is essential to perform monitoring and implement transparency and ethical governance together with the technical solutions.
